---
description: Execute one feature through the Front First pipeline
agent: front-first
---

# Front First

Execute the Front First delivery pipeline for this request:

$ARGUMENTS

Keep scope to one independently demoable vertical slice. Use the specialized Front First subagents. Do not begin backend implementation until the frontend gate passes and the contract is frozen. Return command evidence, not confidence statements.
