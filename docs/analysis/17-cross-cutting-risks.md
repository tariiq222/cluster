# 17 · المخاطر الشاملة وخارطة الأولويات

> **آخر تحديث:** 2026-08-14
> **مطابقة الكود:** `main` @ `c3b17a7`
> **مرحلة المشروع:** تثبيت بعد تقليص النطاق إلى نواة المهام المؤسسية.
> **readiness_state:** `unassessable` لإغلاق المنتج — لا يوجد goal charter معتمد من المستخدم الحالي.

## ملخص القرار

النواة الحالية ملزمة على `main`: الهوية، المنظمة، الصلاحيات، المهام المستقلة بنطاق، الوثائق، الإشعارات، التدقيق، الإعدادات، البحث والتقارير للنواة فقط. `WorkRecords` و`WorkDefinitions` و`Workflow` متقاعدة. حارس الحدود نجح في 2026-08-14: 38 اختبارًا / 641 assertion.

مخاطر يوليو 2026 (عقد Business Calendar، فشل `OrganizationDrawers`، مفاتيح `TABLE_OWNERS` الزائدة) لم تعد وصفًا للنظام الجاري. لا تُعاد فتحها إلا بدليل جديد.

## النتائج الحالية

| الأولوية | الحالة | الادعاء | الدليل | الأثر | الثقة | الإجراء الأصغر المسؤول |
|---|---|---|---|---|---|---|
| — | passed | وحدات Work Management غائبة عن التشغيل والعقد والمسارات. | `TaskCoreScopeTest` و`ModuleBoundariesTest` ناجحان في 2026-08-14؛ لا توجد أدلة `apps/api/Modules/{WorkRecords,WorkDefinitions,Workflow}`. | المنتج الجاري لا يعرّض سجلات العمل أو سير الموافقات. | عالية | لا شيء. أي إعادة إدخال تحتاج ميثاقًا جديدًا. |
| — | passed | قائمة المهام الإدارية ونطاق المنظمة منفّذان في العقد والكود والواجهة. | `ListTasksView`، `view=scope`، `owner_organization_unit_id`، `explicit-denies` في OpenAPI والعميل المولّد؛ `TasksScreen` يعرض «مهامي / مهام نطاقي»؛ `OrganizationResourceFacts` و`TasksPermissionsTest` موجودان. | مسؤول مخوّل يرى مهام نطاقه بأسماء بشرية. | عالية للتنفيذ؛ متوسطة للرحلة الحية لأن E2E الكامل لم يُعد تشغيله في 2026-08-14. | إعادة تشغيل البوابات الواسعة عند تغيير العقد أو الصلاحيات. |
| P1 | blocked | ترحيل W27 الإنتاجي يبقى مشروطًا بنسخة احتياطية واستعادة معزولتين. | `W27RetireWorkManagement` يرمي `work_management_retirement_requires_verified_backup_and_restore`؛ `docs/operations/ha-dr-backup.md` يفرض الدليل خارج المستودع. | لا يجوز تطبيق التقاعد على قاعدة قائمة بلا دليل مشغّل. | عالية | لا تُصنَّع معرفات دليل. المشغّل يوفّر backup ID وrestore-validation ID. |
| P2 | observed | 41 عملية OpenAPI و6 موديولات ما زالت مخططًا. | `docs/design/PAGES.md` و`module-catalog.md`: RecordsGovernance, Collaboration, Strategy, PortfolioProjects, Risk, Workspace. | ليست فجوة في النواة الحالية؛ هي نطاق مستقبلي صريح. | عالية | تبقى خارج النطاق حتى يُعتمد ميثاق يضمّها. |
| P2 | observed | `main` المحلي يتقدم على `origin/main` بـ 21 كومِتًا. | `git status -sb` في 2026-08-14: `main...origin/main [ahead 21]`. | العمل الملزم موجود محليًا فقط حتى يُدفع بتفويض صريح. | عالية | لا دفع في هذا التقييم. |
| P2 | unverified | لم تُعد تشغيل بوابات `make test-api` و`make test-web` وE2E في جلسة 2026-08-14. | شُغّل فقط `ModuleBoundariesTest` + `TaskCoreScopeTest`. | نجاح الحراس لا يغني عن البوابة الكاملة قبل الإصدار. | عالية لغياب الدليل | شغّل البوابات عند تجهيز الإصدار أو الدفع. |

## مخاطر أُغلقت ولم تعد سارية

1. تقاعد Work Management من المسارات والمزودات والواجهة والعقد.
2. قائمة المهام بنطاق إداري وتسميات بشرية ومبدّل «مهامي / مهام نطاقي».
3. حقائق المنظمة تُبنى من المورد المستهدف لا من جلسة المستدعي.
4. `OrganizationDrawers.test.tsx` لم يعد موجودًا؛ مسار المنظمة الحالي تغطيه اختبارات الشاشات والتبويبات.
5. حارس الحدود يمر على الشجرة الحالية.

## ترتيب العمل التالي

1. اعتماد ميثاق هدف إذا كان المطلوب حكم إغلاق منتج.
2. تشغيل البوابات الواسعة قبل الدفع أو الإصدار.
3. إبقاء W27 الإنتاجي مغلقًا حتى يوجد دليل نسخة واستعادة حقيقيان.
4. عدم تنفيذ الموديولات الستة المخططة إلا بميثاق جديد.

## الحدث الذي يغيّر التقييم

اعتماد goal charter، دفع `main`، نتيجة بوابة كاملة بتاريخ جديد، أو توفير دليل W27 الإنتاجي. عندها يُحدَّث [`SUMMARY.md`](SUMMARY.md) وهذه الوثيقة.
