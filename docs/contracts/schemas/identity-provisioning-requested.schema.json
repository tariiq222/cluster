{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://contracts.cluster.example/schemas/identity-provisioning-requested.schema.json",
  "title": "Identity Provisioning Requested Event Data",
  "type": "object",
  "additionalProperties": false,
  "required": ["person_id", "person_version", "requested_account_status", "access_context", "classification"],
  "properties": {
    "person_id": {"$ref": "#/$defs/uuidv7"},
    "person_version": {"type": "integer", "minimum": 1},
    "requested_account_status": {"type": "string", "enum": ["pending", "disabled"]},
    "access_context": {"$ref": "access-context.schema.json"},
    "classification": {"const": "confidential"}
  },
  "$defs": {
    "uuidv7": {"type": "string", "format": "uuid", "pattern": "^[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$"}
  }
}
