# Baseline

> بذرة. المهمة ١ من [خطة أساس الثيم الموحّد](2026-08-01-unified-theme-foundation.md) تستبدل هذا الملف بالحالة المقيسة فعلياً لكل بوابة تحقّق قبل أي تعديل.
>
> الغرض: لا يمكن الحكم على أثر التغيير بلا خط أساس مسجّل. أي ادعاء «الاختبارات تمر» بلا هذا الملف ادعاء بلا دليل.

## الحالة المتوقّعة عند التسجيل

| البوابة | الأمر | المتوقّع |
|---|---|---|
| الأنواع | `cd apps/web && npx tsc -b` | خروج 0 |
| الفحص | `npm --prefix apps/web run lint` | 10 تحذيرات، 0 أخطاء |
| الوحدة | `npm --prefix apps/web run test:unit` | 9 اختبارات / 3 ملفات |
| المتصفّح | `npm --prefix apps/web run test:e2e:list` | 52 اختبار / 14 ملف |
| التغطية | `npm --prefix apps/web run coverage` | **`0/0` — البوابة معطّلة فعلياً** حتى تُصلَح في المهمة ١ |

## تغطية

_يملؤها التنفيذ._

## e2e بعد إعادة التوجيه

_يملؤها التنفيذ (المهمة ١١)._
