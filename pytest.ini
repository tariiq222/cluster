[pytest]
testpaths = tests/ops
addopts = --strict-markers
markers =
    unit: isolated host-input and redaction behavior
    integration: deterministic read-only host probe behavior
    e2e: command-line flow across checked-in contract artifacts
